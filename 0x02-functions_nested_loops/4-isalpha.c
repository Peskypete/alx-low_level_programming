#include "main.h"
/**
 *_isalpha:checks for alphabetical char
 *@c: the character being checked
 *Return: (1) if an alphabet, (0) otherwise
 */
int _isalpha(int c)
{
	if ((c >= 65 && c <= 90) |i| (c >= 97 && c <= 122))
	{
	return (1);
	}
	return (0);
}
